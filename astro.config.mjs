// @ts-check
import { defineConfig } from 'astro/config';

// 部署到 GitHub Pages 时改成：
// site: 'https://<你的用户名>.github.io'
// base: '/<仓库名>'（若仓库名为 <用户名>.github.io 则不需要 base）
export default defineConfig({});
